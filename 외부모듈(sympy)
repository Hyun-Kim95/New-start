import sympy

a = list(sympy.primerange(100,1000))
count = 0
for b in a:
    count += 1

print(count)
#python sympy모듈
# isprime(n): n이 소수인지(True) 아닌지(Fales)
# primerange(a,b): 범위의 모든소수 목록을 생성
# 기타 등등
